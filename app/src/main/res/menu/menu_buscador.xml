<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/mnu_nav_buscador"
        android:icon="@drawable/ic_baseline_person_search_24"
        android:orderInCategory="10"
        android:title="search_title"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="collapseActionView|always" />

    <item
        android:id="@+id/search_by_name"
        android:orderInCategory="100"
        android:title="Buscar por nombre"
        android:visible="true" />

    <item
        android:id="@+id/search_by_oficio"
        android:orderInCategory="90"
        android:title="Buscar por oficio"
        android:visible="true" />

</menu>